package com.example.videols.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.example.videols.entity.${s};
import com.example.videols.mapper.${s}Mapper;
import com.example.videols.service.${s}Service;
import org.springframework.stereotype.Service;

@Service
public class ${s}ServiceImpl extends ServiceImpl<${s}Mapper, ${s}> implements ${s}Service {
}
